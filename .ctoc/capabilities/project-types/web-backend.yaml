# web-backend — an HTTP API/server. Runs and answers a health probe (honest:true).
# Security is REQUIRED (it faces the network); it ships a Dockerfile + OpenAPI + migrations.
# DETECTION (DEF5): fastify.config.js was removed — it is fabricated. Fastify ships NO
# root config file (fastify.yaml itself declares files: []), so the marker could never
# fire; it is dropped rather than left as dead data.
projectType: web-backend
detectionMarkers: [nest-cli.json, Procfile]
frameworks: [express, fastify, nestjs, django, fastapi, spring-boot, gin]
priority: 30
runShape: server
phases: { lint: required, typecheck: required, test: required, security: required, coverage: recommended }
run: { strategy: server-probe, honest: true }
configScaffold: [package.json, tsconfig.json, Dockerfile, openapi.yaml, migrations]
verified: web-2026-07
